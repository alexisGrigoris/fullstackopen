sequenceDiagram
    participant browser
    participant server

    Note right of browser: User enters a new note and clicks "Save" button

    Note right of browser: JavaScript intercepts form submission and prevents default behavior

    Note right of browser: New note is added to the local state and UI is updated

    browser->>server: POST /exampleapp/new_note_spa
    activate server
    server-->>browser: 201 Created response
    deactivate server

    Note right of browser: Browser remains on the same page and no reload occurs

         